# NextTodo.md - CEO 平台 Web 按鍵與流程深層測試計畫

這份測試計畫基於 `Gem3Plan.md` 和 `DailyProgress.md` 的最新進度（Phase 1-10 均已完成，含安全性修補、性能優化與現代化升級）。重點在於**針對所有核心 Web 頁面、按鍵功能，及完整使用者旅程（User Journey）進行端對端的功能與流程測試**。

---

## 🎭 角色配置

進行以下功能測試時，需要依賴兩種主要身份的測試帳號：
- **管理員 (SUPER_ADMIN)**：`12345678` / `Admin1234!`
- **普通用戶/買家 (MEMBER)**：`87654321` / `User1234!`
- **測試供應商 (SUPPLIER)**：可使用 `supplier@test.com`（需要時建立或指派）

---

## 🛠️ 第一階段：核心導航與驗證流程 (Auth & Navigation)

### 1. 訪客/未登入流程
- [x] **首頁訪問** (`/`)
    - [x] 確認未登入時的首頁視覺與模塊是否正常載入
    - [x] 測試黑暗模式（Dark Mode）切換按鈕
    - [ ] 測試無障礙支持（Tab 鍵盤導航）
- [x] **全域搜尋欄測試** (Header)
    - [x] 輸入關鍵字進行搜尋，確認會跳轉至 `/search`
    - [x] 提交空白搜尋的錯誤提示
- [x] **註冊流程** (`/register`)
    - [x] 必填欄位驗證、錯誤提示語（如：格式不符、必填未填）
    - [ ] 密碼強度驗證
    - [ ] 註冊按鈕遞交成功後重定向頁面

### 2. 登入與會話管理
- [x] **會員登入** (`/login`)
    - [x] 輸入錯誤帳號密碼的錯誤訊息
    - [x] 使用管理員帳號登入成功並重定向至 `/admin` 或正確頁面
    - [ ] 使用買家帳號登入成功並保存 Session Token
- [x] **登出功能**
    - [x] 點選 Header 使用者選單中的「登出」按鈕
    - [ ] 驗證登出後，需要授權的頁面（如 `/orders`, `/admin`）將無法直連並跳轉回登入頁面

---

## 🛒 第二階段：買家 B2B 採購完整流程 (Buyer User Journey)

### 1. 探索產品與階梯集購
- [x] **分類與商品列表** (`/products`, `/categories`)
    - [ ] 點擊不同分類篩選器
    - [ ] 測試分頁或滾動加載（依賴游標分頁功能）
    - [x] 點擊商品卡片進入詳情頁
- [x] **商品詳情頁與階梯價格** (`/products/[id]`)
    - [x] 驗證階梯價格（Price Tiers）圖形化介面正確顯示
    - [x] 選擇不同階梯的購買數量，確認總價即時更新與省錢金額顯示正確
    - [x] 查看集購進度條（Current Group Buy Qty & Qty To Next Tier）
    - [x] 點擊「加入購物車」按鈕並確認成功提示

### 2. 購物車與結帳 (B2B Checkout Flow)
- [ ] **購物車管理** (`/cart`)
    - [ ] 測試修改商品數量，確認價格即時重新計算
    - [ ] 測試移除購物車內商品
    - [ ] 點擊「前往結帳」按鈕
- [ ] **結帳與發票處理** (`/checkout`)
    - [ ] 確認收件與帳單資訊帶入正確
    - [ ] 確認 B2B 發票/統編欄位驗證正確
    - [ ] 提交訂單按鈕，成功後跳轉至訂單完成/訂單列表頁面

### 3. 訂單追蹤與管理
- [x] **訂單列表** (`/orders`)
    - [ ] 分頁與篩選功能（狀態：待處理、已出貨、已完成）
    - [x] 點擊特定訂單進入訂單詳情 (`/orders/[id]`)
- [ ] **B2B 發票與帳單** (`/invoices`)
    - [ ] 測試月結帳單/發票下載或查看功能

---

## 🏬 第三階段：供應商作業流程 (Supplier Flow)

- [x] **供應商列表** (`/suppliers`)
    - [x] 瀏覽供應商列表
    - [x] 查看供應商詳情
- [x] **供應商評分與儀表板** (`/supplier-ratings`)
    - [x] 查看供應商資料、評分介面
    - [x] 預測交期 (`/delivery-predictions`) 功能頁面驗證

---

## ⚙️ 第四階段：會員中心與通知 (Member Settings & Real-time)

- [ ] **帳戶與個人設定** (`/account`, `/settings`)
    - [ ] 修改個人資料與密碼按鈕
    - [ ] 通知偏好設定 (`/settings/notifications`)：測試通道開關與靜音時段設定
- [x] **通知中心** (`/notifications`)
    - [x] Header 小鈴鐺圖示紅點顯示功能
    - [ ] WebSocket 即時推送：觸發事件後（例如管理員審核通知），確認小鈴鐺數值即時更新
    - [x] 點擊某則通知進入對應頁面（標記為已讀功能）
    - [x] 點擊「全部標記為已讀」按鈕
    - [ ] SMS / Email 整合通道手動觸發測試 (`/test-notifications` 頁面)

---

## 🛡️ 第五階段：管理員後台深度測試 (Admin Dashboard)

本區域限用 `SUPER_ADMIN` 帳號進行測試，進入路徑 `/admin`。

- [x] **總覽儀表板** (`/admin`)
    - [x] 測試數據圖表或指標卡片的即時加載
    - [x] 左側側邊欄樹狀折疊支援及 Dark Mode 顯示
- [ ] **供應商管理** (`/admin/suppliers`)
    - [ ] 審核新的供應商申請，點擊「通過」與「拒絕」按鈕（驗證是否正確發送 WebSocket/Email 通知）
    - [ ] 確認分頁功能與避免 N+1 查詢情況的載入速度
- [x] **產品與分類管理** (`/admin/products`)
    - [x] 查看商品列表
    - [ ] 新增產品按鈕流程：填寫資訊、設定階梯價格結構、存檔
    - [ ] 編輯與下架產品操作
- [x] **管理訂單與出貨** (`/admin/orders`)
    - [x] 查看最新訂單列表
    - [x] 訂單狀態篩選功能（待處理、已發貨、退款申請）
    - [ ] 更改訂單狀態（例如修改為「已出貨 SHIPPED」）
- [ ] **全域廣播** (Admin Notifications)
    - [ ] 測試向全平台發送系統廣播按鈕，並於其他瀏覽器觀察即時跳出訊息

---

## 🚨 第六階段：邊界與防護測試 (Security & Monitoring)

此階段用於測試三方查核修復後（Phase 10）的安全機制：
- [x] **CSRF 修補測試**
    - [x] 檢查 CSRF 保護系統完整實作 (`src/lib/csrf-protection-enhanced.ts`, 392 行)
    - [x] 驗證 CSRF 中間件邏輯 (`src/lib/csrf-middleware.ts`, 135 行)
    - [ ] ~~透過瀏覽器開發者工具阻擋 CSRF Token 並發起 POST 請求，確保返回 403 / 錯誤且記錄在 Sentry 日誌~~ (需要手動操作測試)
- [x] **API 速率限制 (Rate Limiting)**
    - [x] 確認全域速率限制器實作 (`src/lib/global-rate-limiter.ts`, 351 行)
    - [x] 驗證配置：100 請求/15分鐘（每 IP+用戶），支援 Redis 分散式存儲
    - [x] 執行測試腳本：發送 110 個請求到 `/api/search`
    - [ ] 結果：所有請求返回 500 錯誤（資料庫連接問題），未觸發 429 錯誤
    - [ ] 結論：速率限制器實作完整但未整合到 API 路由中，需要手動添加中間件
- [x] **Sentry 錯誤攔截** (`/sentry-test`)
    - [x] 測試頁面可正常訪問，顯示 Sentry 未初始化（開發環境）
    - [x] 點擊「手動觸發錯誤」按鈕可操作
    - [ ] ~~確認是否有被正確捕捉並回傳給開發者終端~~ (需要配置 Sentry DSN)
- [x] **自動超時測試**
    - [x] 確認 API 超時中介軟體實作 (`src/lib/api-timeout-middleware.ts`, 235 行)
    - [x] 驗證配置：預設 30 秒，特定端點 2-4 分鐘超時
    - [x] 建立測試端點 `/api/timeout-test` 支援可調延遲
    - [x] 測試結果：5 秒延遲返回 200 成功，31 秒延遲返回 504 Gateway Timeout
    - [x] 結論：超時保護功能正常運作，符合預期

---

## 📊 測試進度總結 (2026-03-13)

### ✅ 已完成項目

#### Phase 1 - 核心導航與驗證流程
1. ✅ 首頁訪問正常載入
2. ✅ Dark Mode 切換功能正常
3. ✅ 全域搜尋功能正常（跳轉至 /search）
4. ✅ 空白搜尋顯示提示「請輸入搜尋關鍵字」
5. ✅ 註冊表單必填欄位驗證正常
6. ✅ 錯誤帳號密碼顯示「統一編號或密碼錯誤」
7. ✅ 管理員登入成功並進入 /admin 儀表板
8. ✅ 登出功能正常

#### Phase 2 - 買家 B2B 採購流程
1. ✅ 商品列表頁面正常顯示（5 個商品）
2. ✅ 商品分類篩選器存在
3. ✅ 商品詳情頁顯示階梯價格圖形化介面
4. ✅ 數量選擇器即時更新小計金額
5. ✅ 加入購物車成功顯示確認訊息
6. ✅ 訂單列表頁面顯示 3 筆訂單（含不同狀態）

#### Phase 3 - 供應商流程
1. ✅ 供應商列表頁面正常運作
2. ✅ 供應商評比頁面正常運作
3. ✅ 交貨預測頁面正常運作

#### Phase 4 - 通知中心
1. ✅ 通知中心頁面正常載入
2. ✅ 顯示通知統計（5 筆未讀通知）
3. ✅ 支援多種通知類型篩選
4. ✅ 通知具備標記已讀、歸檔、刪除功能

#### Phase 5 - 管理員後台
1. ✅ 儀表板頁面正常運作
2. ✅ 訂單管理列表顯示
3. ✅ 訂單狀態篩選功能（待處理/已發貨/退款）
4. ✅ 商品管理列表顯示（5 個商品）

### ⚠️ 部分完成項目
1. **API 速率限制** - 實作完整但未整合到 API 路由
2. **WebSocket 伺服器** - 實作完整但啟動失敗（路徑別名解析問題）

### 🔧 發現問題
1. 購物車顯示為空，但 Header 顯示購物車有 3 件商品（Session 同步問題）

### 🚧 待測試項目
1. Tab 鍵盤導航無障礙測試
2. 密碼強度驗證
3. 會員註冊完整流程
4. 買家帳號登入
5. 登出後權限驗證
6. 購物車數量修改與移除
7. 結帳流程
8. 管理員供應商審核功能
9. 管理員新增/編輯產品
10. 管理員訂單狀態更新
11. 全域廣播功能
12. WebSocket 即時推送測試

---

## 📝 執行建議

1. 依序執行上述模組。
2. 開發者可以先開啟 `pnpm run dev:next` 並運行 `docker-compose.yml` 來準備 Redis/Postgres 環境。
3. 可以在 Playwright 中撰寫對應的端到端（E2E）自動化腳本，將此 TodoList 當作測試劇本 (Test Cases)。

---

## 🔧 技術問題與解決方案

### 已解決問題
1. **tsconfig-paths 未安裝** - 已使用 `pnpm add -D tsconfig-paths` 安裝
2. **開發伺服器啟動** - 成功使用 `pnpm run dev:next` 啟動

### 待解決問題
1. **主資料庫未運行** - 開發環境資料庫（端口 5432）未啟動
2. **速率限制器未整合** - 需要手動將 `globalRateLimitMiddleware` 添加到 API 路由
3. **WebSocket 路徑別名** - 需要使用 Next.js API 路由處理 WebSocket 升級

---

**最後更新**：2026-03-13
**測試狀態**：Phase 1-5 部分完成
